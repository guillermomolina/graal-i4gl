MAIN
    DEFINE var INT
    LET var = 1
    IF (var = 1 or var = 2) THEN
        DISPLAY "TRUE"
    ELSE
        DISPLAY "FALSE"
    END IF
    IF (var = 1 and var = 2) THEN
        DISPLAY "TRUE"
    ELSE
        DISPLAY "FALSE"
    END IF
END MAIN
